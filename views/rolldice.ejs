<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>RollDice</title>
</head>
<body>
    <h3>Dice gave value : <%= Math.floor(Math.random() * 6) + 1 %></h3>
    <!-- <h3>Dice gave value : <%= // num %></h3> -->
    <h3>Dice gave value : <%= diceVal %></h3>
    <% if(diceVal == 6) { %>
        <h2>Nice! Roll dice again.</h2>
    <% } %>
</body>
</html>